package com.muhammad.twitterCounterAppMvvm.ui.theme

import androidx.compose.ui.graphics.Color

// Light Mode Colors
val PrimaryLight = Color(0xFF1DA1F2) // Twitter blue
val SecondaryLight = Color(0xFF14171A)
val BackgroundLight = Color(0xFFFFFFFF)

// Dark Mode Colors
val PrimaryDark = Color(0xFF1DA1F2) // Twitter blue
val SecondaryDark = Color(0xFFE1E8ED)
val BackgroundDark = Color(0xFF15202B)

val titleColor = Color(
    android.graphics.Color.parseColor(
        "#1C211F"
    )
)

val copyTextColor = Color(
    android.graphics.Color.parseColor(
        "#00A970"
    )
)

val clearTextColor = Color(
    android.graphics.Color.parseColor(
        "#DC0125"
    )
)

val backgroundColor = Color(
    android.graphics.Color.parseColor(
        "#E6F6FE"
    )
)

val twitterColor = Color(
    android.graphics.Color.parseColor(
        "#E6F6FE"
    )
)
